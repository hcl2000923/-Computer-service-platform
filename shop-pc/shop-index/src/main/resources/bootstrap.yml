spring:
  mvc:
    view:
      prefix: /WEB-INF/
      suffix: .jsp
  application:
    name: shop-index
  cloud:
    config:
      uri: http://localhost:8888
      label: master
#开启降级
feign:
  hystrix:
    enabled: true

#tomcat.virtual.path: /**
#basePath: D:/shopping